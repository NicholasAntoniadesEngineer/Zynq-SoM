# PLATFORM REWORK — overnight campaign tracker (branch: platform-rework)

*Live per-unit status. Resume here. Mandate: docs/AI_PLATFORM_ROADMAP.md to the final
product; NO master merge until user approval. Baselines: carrier pcb md5 e405097b
(188x165, deterministic, all gates green); schematic goldens locked (user: golden).*

## Units
| # | Unit | Status |
|---|---|---|
| U1a | Project spec v1 (carrier/project.json + loader) + extract _WIRED_SHEETS | DONE |
| U1b | Extract _PILOT_PROX_SHEETS + SOM pose defaults to spec | DONE |
| U1c | Declarative module-face anchors (kill b.name=="power_som") + reg-band prefixes | DONE |
| U1d | Byte-identity proof through the spec path (md5 e405097b) | DONE |
| U1e | ModuleGeom: module pcb/interface paths from spec (generalize SomGeom entry) | DONE via PROJECT_ROOT indirection (29 modules) |
| U1f | --project CLI + CARRIER path resolution via project root | DONE (env pre-parse, loud fail, identity 2f13efe8) |
| U1g | devkit_mini second-project proof: green board, ZERO engine edits | NEXT-STEP #1 (authoring campaign; recipe in RETROSPECTIVE) |
| U2a | Defect-injection corpus library + gate-fires proof per class | DONE (13 tests, 1.1s) |
| U2b | Fast flow/facing synthetic harness (validated vs corpus + visual sample) | pending |
| U3a | Wire contracts: coverage 19 -> 0; cascade fixes mechanized | DONE: 23/23 wired, EXIT 0 all gates, 215x168, deterministic 2f13efe8 |
| U4a | board_verdicts.json always-on structured verdicts | DONE (verified written) |
| FIN | Deep retrospective vs all user asks + decided next steps | DONE: docs/PLATFORM_REWORK_RETROSPECTIVE.md |

## Log
(newest first)
- WAVE-27..33 LAWS CLOSED ACROSS THE CODEBASE (merged 79c3d68, 51c6c4b, cc63134, e337af4, 557b6db, 0205c1d, b3d4253): every wave BYTE-INERT on both boards (carrier `06308484dd95ffb65b09ef456bd64547` 168x163, devkit `8eecadfde74d523af5f185812f62163c` 100x100); suite 1776 -> **1824 passed / 5 skipped**, ruff clean repo-wide. (1) COMMENT LAW CLOSED: over-cap functions **364 -> 0** across schgen/subsystems/carrier/devkit_mini/examples/scripts, all trees now <=1.2% prose. Datasheet provenance was CONVERTED, not deleted — 101 subsystem + 141 carrier + 44 devkit registrations behind gates. The registry immediately caught a SHIPPED false provenance: `CAMERA_I2C_PULL`/`LCD_TOUCH_PULL` cited a 3.5 kOhm UM10204 ceiling then called 4k7 "inside the band" — re-derived from Rp(max)=tr/(0.8473*Cb), 3.5k corresponds to Cb=101 pF and 4k7 holds only to Cb<=75.3 pF, so the prose was arithmetically FALSE. A comment would never have been re-derived. (2) BUILD LEDGER LANDED: a full build printed 12 lines, now emits **226 ordered lines / 179 declarations over 9 census files** to `<project>/reports/build_ledger.txt`, byte-identical across consecutive builds, empty-baseline gate with four failure modes (declared-but-absent, undeclared, BURIED-in-function, stale cover), proven to bite. Source class (physical/datasheet/standard/policy/fitted) lives in the declaration table and CANNOT be passed at the call site; ASSUME rows resolve the LIVE module attribute so a ledger line cannot disagree with the code; basis capped at 88 chars. LAW-5 SETTLED: budget 16382.6 mm rejected **0 of 1742** candidates in BOTH passes — the binding constraint is the PACK (1072/1083). `CROSS_K` is INERT as a sizing constraint, declared `fitted` with the basis "fitted from two boards; NOT a physical law". (3) LANDMINES CLOSED: `ZONE_FILL=0.58` declared-and-never-read while a bare `0.62` was live (twice); `SOM_DECOUPLING_INSET` hand-inlined FOUR times inside the estimator that SIZES the board; `EDGE_MARGIN` three times by value; the emitted board's own `(general (thickness))` a bare `1.6` shadowing the ledger-registered `STACK_THICKNESS_MM`. The suspected triple-derived decoupling count was MEASURED and DID agree (18 caps, grid 38.0x30.0) — no live defect, now single-owned by construction anyway. (4) A LAW-0 GATE THAT GREPPED PROSE: `copper_debt._where` resolved 6 of 7 anchors by substring-matching COMMENT TEXT and returned an `ANCHOR-NOT-FOUND` marker STRING instead of raising, so two workers had to RESTORE deleted prose to keep it alive. All ten anchors now bind to structure or a registered basis name; `_where` raises on a missing anchor AND on one resolving to a comment/docstring, so it cannot be re-laid. (5) BASIS MACHINERY was TRIPLICATED by the orchestrator running two prose workers in parallel without agreeing a home — consolidated to `schgen/core/basis.py` (66 lines, zero values/parts/nets), 137 duplicated lines removed, stricter half of each validator kept. (6) SYNC-DUPLICATE HARDENING: iCloud made 73 duplicates during ONE operation including `test_ethernet 2.py` which pytest would COLLECT; `.gitignore` blocked `* 2.*`/`* 3.*` but nothing beyond. One named predicate (`core/artifacts.py`) + a root `conftest.py` rejecting them at the collector — note `pytest_ignore_collect` is FIRSTRESULT and must return None to abstain, never False (False overrides `norecursedirs` and made the suite descend into `.claude/worktrees`, 155 collection errors). OPEN, user decision: ~25 unpushed commits; ~15 decision-bearing literals in `emit.py::_class_dict`; a via-only 0.45 mm `.kicad_dru` rule (via-to-via has 0.15 mm headroom) that would close the hand-edit hole-to-hole gap.
- CORRECTION to the wave-33 commit message: it called the ~75 `emit.py::_design_settings` literals "KiCad SERIALIZATION defaults, not engineering decisions". Measured: 138 literals there, **78 cosmetic and 60 DRC/fab-shaped** (`min_clearance 0.09`, `min_track_width 0.09`, `min_via_diameter 0.3`, `min_via_annular_width 0.05`, zone `min_clearance 0.127`/`min_thickness 0.25`, the track-width ladder). Those ARE decisions. The alarming reading is also wrong though: `schgen/verify/fab_profile.py` already gates emitted DEMAND against CITED JLCPCB capability and passes on all six metrics, and it explicitly discloses the one permissive rule — "(.kicad_pro min_via_annular_width DRC floor: 0.0500 mm — permissive rule, not what the board emits)" against a 0.0750 demand meeting a 0.0750 floor. So they are decisions, and they are guarded, but they are not ledger-declared. Making the emitted DRC derive from the fab profile is a DESIGN choice (equal to the floor, or floor+margin as wave-32 did for hole-to-hole), not a mechanical dedup — user decision.
- WAVE-26 LINK SILENT FALLBACK (merged dcf47ff): `schgen link` on a CLEAN tree rewrote **486 refdes lines across 22 carrier sheets** (U16001 -> U17001) and stripped **568 lines** — the whole `board.design_settings` block — from the `.kicad_pro`. `schgen board` on the same tree produced ZERO churn. Anyone running `link` and committing would have churned every designator on the board. ROOT CAUSE 1: `board.build_board` carried `idx = sheet_index.get(sc.name, i) if sheet_index else i`, where idx is the stable refdes band feeding `uniquify()`. `schgen board` passed the index from `<project>/sheet_index.json`; `link` passed NOTHING and silently fell back to the ENUMERATION POSITION. Fixed by giving the assignment ONE owner (`project.stable_sheet_index`) and making `sheet_index` a REQUIRED parameter whose lookup RAISES on a missing sheet. The two callers that genuinely want positional numbering (selftest's temp fixtures, generated `examples/devkit_mini`) now say so through `project.positional_sheet_index` — deliberate, named, unreachable by omission. ROOT CAUSE 2: `build_board` wrote a MINIMAL `.kicad_pro` ({meta, erc}); `design_settings` is added later by the PCB stage, which `link` never reaches, so its write DESTROYED the last full build's settings. `build_board` now MERGES, preserving sections it does not own — safe because `pcb/emit.py:write_project` replaces `design_settings` wholesale every run. PROVEN: `link` leaves a clean tree on BOTH projects (0 churn, was 22 files); both boards byte-identical; 1776 passed. Also: `.gitignore` blocked `* 2.*` and `* 3.*` but nothing beyond, so **114 iCloud `… 4.png` duplicates** accumulated in `carrier/renders/assembly` and failed `test_committed_stage_pngs_are_current`; pattern widened to `* [0-9].*`, 424 purged repo-wide (none were tracked).
- WAVE-25 ONE ROTATION CONVENTION + DERIVED DF40 ESCAPE (merged 1fa4ac7): both boards BYTE-IDENTICAL, DRC 0, 1776 passed; across 58 gate verdicts exactly ONE key moves (`quantize_census.n_files` 13->14, the deliberate addition of `pcb/turn.py`). HANDEDNESS: `mating_face` carried two rotation conventions of OPPOSITE handedness — pad/point maths turned by -R while `_rot_bbox` mapped (x,y)->(-y,x) at 90, the inverse — reconciled only by `_rot_bbox_cw`'s `360.0 -`, an unnamed flip; and `placement.py` already had FOUR live callers of the raw form beside seven correct ones. New `pcb/turn.py` (`KICAD_TURN_SIGN` + `turn_point`/`turn_box`/`pad_half_extent`); both old helpers DELETED, 33 call sites migrated. WHY IT HID: the two forms are IDENTICAL for a bbox symmetric about the footprint origin — **45 of 62 parts**; the 16 asymmetric ones are the CONNECTORS. Measured: 1619 turn_box calls, 126 rotated, **8 genuinely disagreed** (worst 2.800mm SFW15R-1STE1LF@270). The board still does not move because zone SIZE is convention-invariant (the expression collapses to a width) and connector POSITION is re-derived by the edge-seat stage from correct pad maths — so LATENT, not active. The orchestrator's board-area hypothesis was REFUTED by that measurement, not accepted. PAD BBOX: `footprint.py` and `embed.py` each carried the same `% 180`+swap, correct only for quadrant multiples — a 2.0x1.0mm pad at 45 deg under-reported **52.86% on y**. FALLBACK: `_footprint_bbox`'s 2x2mm stand-in now RAISES (95 footprints scanned across both projects, zero hit it). ESCAPE: six hardcoded DF40C-100DP dimensions the module already parsed, three of them rounded 3nm off their own footprint, now all derived; `PITCH_TOL_MM=0.001` replaces a `1e-6` that compared a nominal 0.4 against EasyEDA-rounded 0.3999 (5x above worst export deviation, 65x below the smallest real distinction). DP output identical; on the DS variant the old literals put all six escape corridors **0.020mm INSIDE the pad copper** (handed to the composition legalizer as keepouts nothing compares to the footprint), misplaced 230/293 lane ports, left the coexistence window 0.265mm too shallow, and gave `_self_check` — the LAW-0 connectivity proof — a pad box 0.02mm short at each end of every GND pad. TWO CENSUS CLAIMS REFUTED BY MEASUREMENT (recorded so they are not re-raised): the `1e-6` pair branch DOES fire (6x on som_j2 — the carrier carries the DP plug whose GND columns sit on exact 0.4 multiples), and "vias inside pads with no gate firing" does NOT reproduce (every seat is gated by `_via_feasible` against real parsed pad boxes). `test_turn.py` run against shims of the old code: **14 of 40 FAIL** — the tests catch the defect rather than passing beside it.
- WAVE-24 BLOCK DIAGRAM PROJECT SCOPE (merged 3c212bc): closes a landmine this tracker has listed since wave-5 ("block_diagram.svg REPO_ROOT clobber"). Both projects wrote to the single repo-wide `docs/block_diagram.svg`, so whichever built last silently replaced the other's — observed live, the devkit's 12-sheet diagram replacing the carrier's 37-sheet one. Now `PROJECT_ROOT/docs/block_diagram.svg`, matching `reports/`, `manufacturing/` and `renders/`. git records a pure RENAME (b6251c2bf58297786e3f2bd525786a61) so the carrier diagram is byte-identical through the move; the shared path is deleted and written by neither build. `devkit_mini` artifacts also refreshed to carry the wave-23 SI fix, which had only reached the carrier (`usb_hs_pair` 1.27 -> 3.81; Ethernet MDI correctly stays 1.27 per IEEE 802.3 Cl.40 and gains its citation). Both manifests verified: 93/93 and 62/62 artifact hashes consistent.
- WAVE-23 SI UNITS + GATE SCOPE (merged f0fc1cf): board **BYTE-IDENTICAL** `06308484dd95ffb65b09ef456bd64547` — SI tolerances are routing metadata, not placement geometry. 5 of 13 audited intra-pair figures disagreed with their own citations; worst was TOO LOOSE. FMC LPC 1.27 -> **0.127** (VITA 57.1 / Samtec ±5 mil) across 14 pairs = shipped **10x under-constrained**; MIPI CSI-2 1.27 -> 0.508; HDMI TMDS 0.15 -> **2.9972** (CTS `0.15*Tbit` is DIMENSIONLESS — a bit-fraction read as millimetres); USB 2.0 HS 1.27 -> 3.81. One loader (`schgen/core/si_spec.py`) reads the vendored cited table and RAISES on a missing row. Shipped-artifact disagreement **58 of 74 pair nets -> 0 of 74**. `est_via_cost`'s basis had justified its 2-leg premise with the bogus 0.15mm; re-based onto the real tightest budget (0.127mm), which holds it a fortiori — arithmetic never used the figure. TWO GATE-SCOPE DEFECTS of one family: `pytest schgen/tests` collects **860 of 1,720** and `ruff check schgen` passed while `ruff check .` had **2 E501 failures since 2f0fae7** — every "green" report for ~20 merges was scoped too narrowly to see them. Acceptance is now bare `pytest` + `ruff check .` from the repo ROOT (`python3 -m ruff` does not exist here and silently no-ops). Full suite **1736 passed / 5 skipped**. OPEN: `SD_BUS_MATCH_MM` researched to **3.50mm** (AMD ±50 ps halved to ±25 ps per segment for a level-shifted bus — this board has a TXS02612 — over our 7.15 ps/mm stackup); the shipped 2.5 is ~30% over-tight, not unsafe. It lands DERIVED with the F12 stackup consolidation rather than as a second literal. A directional-skew reading of UG933 was CHECKED AND KILLED: current AMD guidance is symmetric ±, and AMD's own ZC702 with the same translator matches all SD lines equally because the translator's ~1.5ns clock-to-channel skew dwarfs any PCB offset.
- WAVE-19..22 PROSE PURGE (merged fc5861e, 57ff9ba; renders c3a627a): schgen **66,399 -> 51,242 lines**, `#` lines 6,198 -> **51**, docstring lines 8,330 -> **9**, functions/classes over the 2-line cap **844 -> 0**. Both boards BYTE-IDENTICAL throughout. Root cause of the earlier failure was the orchestrator's own worker prompts, which capped only `#` comments per file and explicitly permitted "docstrings in repo idiom" — so every worker relocated the prose into docstrings. The standard is not relocation: code so written it needs no description. NOT DONE, measured 2026-08-01 and approved for conversion: `subsystems/` `carrier/` `devkit_mini/` `examples/` still hold **25,761 lines at ~38-41% prose** (10,073 prose lines, 360 over-cap docstrings) — but that is datasheet PROVENANCE, not padding, so it converts to structured basis ON the declaration rather than being deleted.
- WAVE-18 BOTTOM-FIRST (merged c885832): board **168x163 UNCHANGED**, md5 06308484, **B.Cu 165 -> 176 of 507 at ZERO area cost**, airwire 15,946.3/16,383 (97.34%), devkit byte-identical. (1) ROLE INVERSION — the defect the user spotted in four render crops: a bottom-assigned block emits PRIMARY on B.Cu and SECONDARY on F.Cu, but _bottom_zone_shapes' SHELF branch passed the UNMODIFIED top-side classifier map, so the passives the classifier had pushed "to the bottom to relieve top pressure" landed FACE-UP once the block flipped and only big ICs went down. board_aux was 16 F.Cu/2 B.Cu, bringup_rails 22/1 — two ALREADY-declared blocks delivering 3 of 41 parts instead of 27; the contracted path (_lift_face_top) was correct all along, the shelf path meant the opposite. Fixed by making role assignment a REGISTERED SHAPE (bottom-a{asp} = everything but face_top primary; bottom-split-a{asp} = classifier split), role-aware registered first + box de-dup, so equal geometry resolves face-down at zero cost. (2) THERMAL POUR now follows the part to its OWN outer layer: _pour_evidence queried the library-literal layer while the emitter swaps faces for a mirrored instance — asymmetric TLV75725_DYD would have FALSE-FAILED at Tj 123.9 vs 115.0, latent only because fmc is connector-refused and going live under exactly this push. Emitter transforms split by real trigger (_mirror_thermal_spec on inst.mirror, _side_thermal_spec on inst.side). Board byte-inert. (3) CONNECTOR SHIFT CLOSED, GATE-PROVEN NEGATIVE: 13 S-edge relocations, ZERO under 168mm, min buildable 174mm, 2 INFEASIBLE, best row FAILS with DRC 9 malformed_courtyard AND moves 25 parts OFF B.Cu — negative for BOTH goals. Do-not-retry scoped to RELOCATION (S-edge connector on B.Cu at the SAME edge is untested — _pack_edges has zero .side references). Ratchets TIGHTENED: legalize_only 12->4, interior_reseat_retry 92->36. 855 tests. REFUTED: intra-block re-splitting as an AREA lever (recoverable zone-box area 434mm2 = 1.58% of board, contracted sheets 0.00% insensitive, "everything to secondary" is +3.32% WORSE); the aggressive role-aware ladder measured 174x162 (+2.94%) at 99.99% of the LAW-5 budget — the first configuration where AIRWIRE, not packing, plausibly sizes the board.
- WAVE-18 BOTTOM-FIRST (this wave): carrier **168x163 = 27,384mm2 UNCHANGED**, md5 `06308484dd95ffb65b09ef456bd64547`, **bottom-side 165 -> 176 of 507 placements** (569 fps 404/165 -> 393/176), cross-airwire 15,904.7 -> 15,946.3 /16,383 (97.08% -> 97.34%), 29 gates green (DRC 0, D13 110/0, contract 190/0, LAW-6 0/93 face-down), 2 builds byte-identical @11m17s; devkit BYTE-IDENTICAL to the pin 8eecadfde (100x100). THE USER'S DEFECT, root-caused: the SHELF bottom-variant path reused the two-side CLASSIFIER map, and since a bottom block's PRIMARY emits B.Cu, the classifier's "passives to the bottom" population is exactly what presents FACE-UP after the flip — board_aux emitted 16 F.Cu/2 B.Cu and bringup_rails 22/1 while the CONTRACTED power emitted 12/39 through _lift_face_top. Fix: role assignment becomes a registered SHAPE — `bottom-a*` (everything but face_top face-down, the contracted path's own rule) registered BEFORE `bottom-split-a*` (the classifier split), box de-dup dropping the split twin on equal geometry so an equal box resolves toward B.Cu at zero area cost. Only board_aux (-30.3% block area) and mechanical move. REFUSED WITH NUMBERS: the replace form (drop the split twins / prune them as dominated — all of them ARE dominated on the two live sheets) = 174x162 (+804mm2, +2.94%) for ~189 parts, a trade not a win, and the dominated shape is load-bearing for the 168 width (greedy non-monotonicity again). LADDER EXHAUSTED (PLAN-MEASURED): all 7 declarable non-impedance interior sheets at once = 177x163 (+5.36%) with B.Cu DOWN to 142 — every one was OFFERED a bottom variant and the est chooser kept all 7 TOP; pure perturbation. TWO CORRECTNESS LANDINGS RIDING ALONG: (a) the thermal pour credit was asking for the LIBRARY-frame layer while the emitter laid the part's OWN outer layer — latent FALSE-FAIL of TLV75725 on B.Cu at Tj 123.9 vs 115.0; LAYER_SWAP/pour_layers_for now live in the gate and the emitter imports them, with the document-mirror and the face-swap split by their real triggers; (b) NEW LOUD RULE — an impedance-carrying sheet may not take B.Cu while In2.Cu carries no copper (measured: F.Cu 4 zones, In1 1, In2 0, B.Cu 3), so a DP90/DP100 pair face-down has NO reference; DERIVED from DiffGeometry (the est_via_cost derivation) and self-emptying once In2 is filled. (c) FALLBACK CENSUS DOUBLE-COUNT fixed at fallbacks.snapshot's own contract — _choose_conn_shapes left the events of both discarded packs of every REJECTED mirror trial in the ledger; carrier ceilings TIGHTEN legalize_only_compaction 12->4 and interior_reseat_retry 92->36, devkit 6->2 and 76->72, boards byte-identical. CONNECTOR SHIFT CLOSED (gate-proven negative, 13 scenarios): 0 narrower than 168, min width 174, best row FAILS its build (DRC 9 malformed_courtyard, 3 ratchet regressions) and costs 25 parts OFF B.Cu. OPEN: the conn-class veto (144 parts) needs LAW-6 rule (d) extended to conn-class + a bidirectional THT blocker in _pack_one_zone before it can be relaxed; second-reflow MASS is unmodelled (C37004, T10001, L22003, BT3001).
- WAVE-17 BACKTRACKING SEARCH (merged d3a7f0f): carrier 172x163 -> **168x163 = 27,384mm2 (-2.33%)** md5 4f888a20; devkit 123x100 -> **100x100 (-18.7%)** md5 8eecadfde; cross-airwire **IMPROVED** 16,265.2/16,576 (98.12%) -> 15,904.7/16,383 (97.08%); bottom-side **140 -> 165/507** with nothing traded (power back on B.Cu). THE WALL WAS THE SEARCH: _attempt_pack's interior loop was greedy first-fit with NO backtracking, so one block's failed seat killed the whole outline candidate — fatal in 48/48 interior rejects at W in [168,171], yet shrinking that block was worthless and NON-MONOTONE (-20% packs at 171/169, fails at 170/168; -40% fails everywhere; 7 constant-area aspect re-flows buy 0mm) while freeing 5mm at a NEIGHBOURING seat packed all four. _reseat_retry evicts ONE placed block per trial, re-seats it at its chosen shape, restores exactly on failure; _Occupancy.fits UNTOUCHED so every seat was always legal; deterministic by total order; bounded by |placed|; registered loud (interior_reseat_retry=92, ratcheted; legalize_only_compaction tightened 14->12). THE SPEC'S OWN PARAMETERS WOULD HAVE LOST THE WAVE (cap-4 shortlist + depth 2 fails all four widths) — the binding parameter is candidate-pool BREADTH, not eviction depth; depth 1 suffices, depth 2 changes nothing at 10x cost, and the fitted cap constant is deleted. Byte-inert control: budget 0 reproduces master exactly. Perf: exact place_near window (a cell freed by eviction must lie in the evicted block's inflated bbox) cut naive 654s build_plan to 280s bit-identically; full build 443 -> 634s (+43%) — the standing price. 851 tests. PRIOR WAVE REFUTATIONS ABSORBED: the interior critical path is NOT the binder (relieving it 5mm buys 0mm; 25mm buys the same 4mm one unrelated cut gives; row relief and board width measured ANTI-correlated), and every geometric lever on it was refuted. REMAINING FLOOR: the S-edge run at 167.7693mm — W<168 needs a PMOD off the S edge, a USER SPEC DECISION (L-B), never authorised. L-D (EDGE_INSET per-block) measured WORSE (-3mm W).
- WAVES 14-16 — **BOARD 185x162 -> 172x163 = 28,036mm2 (-6.45%)**, md5 aa32a40d, airwire 16,265.2/16,576 (98.1%), bottom 140/507. THREE landings. (a) w14 bb74823 ASSEMBLY REGRESSION: the wave-8 GOVERNANCE commit 7ed5219 silently deleted the assembly hook 17 min after the feature landed (alias `asm_mod.generate` made `git log -S "assembly.generate"` blind); restored + made impossible via an outside-in presence check that FAILS the board, an AST deletion tripwire, and a staleness guard. (b) w15 468fca8 LAW-6 ORIENTATION ORACLE, byte-inert: `_mating_face_out_dir` used a CCW matrix while every geometry kernel uses KiCad's CW one (gerber arbitration: CW 0.0000mm error/pad, CCW 3.54-19.71mm) — they agree at 0/180 and are 180deg apart at 90/270, so any connector on E/W was built MOUTH-INBOARD. It survived only as a COMPENSATING-ERROR PAIR (the four E/W families had their labels hand-flipped to -Y from renders). The tempting one-line table flip would have solved HDMI on E/W **and rotated the RJ45 mouth inboard** — un-pluggable, and the gate could not have caught it (same oracle). Fixed atomically: CW oracle + all 8 vertical cells + all 9 labels RE-DERIVED FROM FOOTPRINT GEOMETRY (8 -> +Y, XT60 -> +X); 16/16 mouths off-board; both boards byte-identical. (c) w16 2e0eb8d THE WIDTH GAIN: `_pair_gap` charged the full 20mm cable gap whenever EITHER neighbour was overmold — double-counting a boot the plain neighbour lacks. Now charged by HOW MANY BOOTS MEET: HDMI<->HDMI keeps 20.0, HDMI<->plain gets the derived 3.0 (plug half-width - 8.000mm measured copper half-width). 13mm of width, ALL of it in the asymmetry. Consumer audit found SIX sites serving FOUR requirements; stage_templates got its own named constant; connector_spacing_gate was STRENGTHENED (the overmold<->non-family pair was policed by NOTHING, now a hard 3.0mm copper rule that bites at 9.27mm). REFUTED + REFUSED with derivations: S edge_order (+0.52% area), EDGE_MARGIN 9.385 (= exactly 10.0-0.615, the amount needed to hit 184 = FITTED; worth 0mm post-L1). 10-agent adversarial brief at scratchpad/width_floor_brief.md refuted 6 hypotheses incl. my own frame-aspect theory. OPEN: plug datum 22.0mm is an UPPER BOUND, no first-party drawing obtainable; airwire at 98.1%; bottom population fell 165 -> 140 (guard kept the conservative plan). NEXT BINDER: the interior horizontal critical path, 167.215mm on the scanline 17.8mm north of the SoM.
- WAVE-13 — THE BOTTOM SIDE FINALLY PAYS (merged e6d9e7d, renders f557f95): board **185x162 = 29,970mm2**, md5 efe462cc, **bottom-side parts 131 -> 165 of 507 (26% -> 33%)**, airwire 16,384.5/17,139 (95.6% of budget). USER-FLAGGED DEFECT: _bottom_shapes vetoed an ENTIRE contracted sheet if its rigid template packed ANY face_top (TP/LED/SW) part on the primary side — all-or-nothing, while shelf sheets already split per-part. That blocked the four biggest interior blocks over a handful of LEDs/testpoints, including `power` — which wave-12 had proven SETS the board height. _lift_face_top moves face=top parts out of the rigid template into the secondary F.Cu pack (primary offsets byte-exact, zone box does not grow on any of the 4 sheets); contract re-measure is the arbiter (0 of 15 lifted parts is a contract member); three layers stop a face-down user-facing part incl. a new hard LAW-6 rule (d) — 0/93 carrier, 0/15 devkit, verified on the emitted board. `power` LANDS: 39 parts top->bottom (3 ICs, 2 inductors, a MOSFET, 23 C, 10 R via KiCad-exact mirrored docs) with TP20001/TP20004/D20001 kept on F.Cu. H floor 163 -> 162 (wave-12's "H set by top-pinned power" superseded — power is no longer top-pinned). usb_jtag rejected on airwire, power_som/uart_bridge inert. 829 tests (+7); punch_free_plan_rejected auto-ratcheted 2->0. OPEN USER DECISION: the guard's (area, est_cross) key trades area for airwire and is now at 95.6% of the LAW-5 budget.
- WAVES 10-12 — THE BOTTOM-SIDE QUESTION, ANSWERED (merges 828c602, 4483208, 8a47556). Board 185x163 = 30,155mm2 (md5 8c093a49), 2 subsystems on B.Cu (board_aux, bringup_rails), airwire 15,319/17,192. W10: BREATHE carried a PRIVATE REPLICA of the D13 crowder rule missing the TP exemption (faked starvation collapsed a real 2.640mm gap to 1.380) -> one predicate; bringup_rails to B.Cu, -555mm2 (-1.81%). Ordinary-via cost = a STEP at 0+, not a curve; class table derived from DiffGeometry, impedance 7.6, ordinary 2.2 INTERIM. W11: punch made EXACT (edge blocks OCC_TOP + per-THT-PAD punch; the old path punched whole FOOTPRINT BBOXes, 2,699mm2 vs 383mm2 of real pad copper = ~7x too conservative board-wide) releasing 8,155.8mm2 = 27% of the board; SoM underside freed; greedy search proven NON-MONOTONE in free area -> monotonicity guard (2 searches, keeps strictly better on (area, est_cross), build 210->300s). W12: instrumented 5 plans x 9 stage boundaries — there is NO est/emission gap worth closing (constant +315mm = 2.06% upper bound, 4/4 rank agreement; post-floorplan movers net -172mm; my DEFECT-3 framing REFUTED and marked wrong in the doc). THE ANSWER: 2,868 outline candidates, 14 packed, LAW-5 budget rejected ZERO — the board is 100% PACK-bound. **W=185 is a geometric floor from the S-edge connector run (184.669mm needed; 184.269 under the best of all 24 orderings) and H=163 is set by `power`, provably TOP-PINNED by user-facing LEDs/TPs** — the one block that can never use the released bottom surface. Bottom-eligibility ladder EXHAUSTED, every refusal loud + declared. Double-sided placement cannot shrink THIS board further: its size is set by the connector perimeter, not by component area. AWAITING USER: (1) area-first pack order = 185x160 (-1.84%) at +9.0% airwire / 98% of budget; (2) guard-key tolerance band (any X in 0-6.99% is byte-inert today AND rejects both risky trades); (3) moving pmod/pmod_expansion off the S edge = 81.2mm of the 164.8mm binding run — a SPEC decision, the only lever that moves W.
- WAVE-9 BOTTOM-SIDE (merges 6c8bd44 P1, 6712d01 P2, 26bef67 chirality; renders db9436d): side is now a per-block DOF and CHIRALITY IS PAID. P1: two-surface occupancy + PUNCH masks, side-tagged mirrored ZoneShape variants, via-cost estimator term, face_top constraints — landed BYTE-INERT (both boards x3). P2: side choice is EST-DRIVEN (`_pick_sided`, estimator + est_via_cost, not anchor distance); it RESCUED user_io from 36 distance-preferred bottoms worth +245mm; achiral opt-ins measured NEUTRAL -> reported, not landed (user rule). CHIRALITY: schgen/generate/pcb/mirror.py materialises pcbnew-LEFT_RIGHT twins as real .kicad_mod docs in a gitignored .mirrored_fp/ cache — a mirrored instance IS an instance of the mirrored document, so every kernel stays side-blind with ZERO mirror branches; ground truth pinned vs pcbnew 10.0.2 FOOTPRINT::Flip at 0.0 nm over 584 fixture pads + kicad-cli DRC 0. First bottom-side win: board_aux (SOT-23-5 + VSSOP-8) on B.Cu, cross-airwire 15,280.3 -> 15,193.0 (-87.3mm) at 185x166 (md5 283ceb64). Honest negatives: debug_boot -229mm2 was flipping JTAG/SWD MATING headers = a LAW-6 token-check hole (closed, loud conn-class raise); bringup_rails -555mm2 blocked by ONE residual top-vs-top D13 crowding (U5001 vs lcd 1.38/1.50) — NEXT LEVER. 799 tests + 13 chirality tests. TRAP: pcbnew FOOTPRINT::Flip SEGFAULTS on a parentless footprint (null BOARD -> BOARD::FlipLayer) — any re-run of the ground-truth harness must add footprints to a BOARD first; the in-repo test uses kicad-cli only.
- WAVE-8 COMPLETE (merges 188993b devkit-power, c8411bd silk-scope, c4e85f5 contracts-census, 19670dd engine; renders 70efb64): carrier **185x166 md5 1becce89, cross-airwire 15,280.3/17,349, contract gate 190/0**. Engine: corridor eviction in the POST-gridify frame; knife-edge subjects 0.000 -> +0.050 under the 4dp gate kernel; FOUR-orientation group live (uart_bridge t180 chosen, rj45 member-mirror ACCEPTED, pmod's rigid -7.7% probe contract-illegal = honest negative); _SNAP_EROSION re-base REFUTED by measurement (0.75 = declared margin); fmc U11001 3/3 curated vias, thermal_via_lattice ceiling 0 HARD-LOCKED. Census: 42 new datasheet-cited structures (47 parts, 14 sheets), coverage UNGATED 88->41, airwire improved. Silk labels = per-project data (devkit leak closed); devkit power hard-wired (64->0, + generic N/A term resolution); ratchet caught a missed baseline within hours; evicted 0-byte pmod.kicad_sch healed. Devkit 123x100 reds {return_path}. Ops lesson: multi-diff hand-merges diverge silently — workers redeliver ONE stacked diff against the exact landing tip. NEXT: wave-9 bottom-side (docs/BOTTOM_SIDE_STRATEGY.md, decisions resolved incl. battery holder).
- WAVE-7 GOVERNANCE + SCANS + ASSEMBLY (merges fc76413 assembly, 1b98a05 governance): user law "NO legacy, NO silent fallbacks" ENFORCED — legacy zone-packer branch DELETED (ZoneInfeasible raises loud, self-tested); 13-transform quantization registry + EMPTY-baseline census gate; 6-path fallback ratchet with honest baselines (carrier cand_cap=82/legalize_only=2/thermal_lattice=3, devkit evict_moved=33/cand_cap=2); 13-stage manifest + may_move tripwires; generated docs/GEOMETRY_PIPELINE.md. Scan A: 61 perturbation sites, 0 landmines (knife-edge + gate-3dp queued). Scan B: 22 fallback paths + 14 mutation sites, 7 silent landmines (top: corridor eviction vs DF40 gridify frame — wave-8). ASSEMBLY feature: bring-up phase order (SoM core -> mate checkpoint -> sections by rail dependency, testpoint checkpoints) + PCBA process steps + 52 deterministic stage renders, both projects, boards byte-untouched. Found: devkit silk prints carrier-keyed labels (chip open). Board unchanged 183x164 b291ce4f; 771 tests.
- WAVE-6 EXACT REACH + FLOOR LAW (merged --no-ff 188c519, renders 7ea19ae): board 188x168 -> **183x164 (30,012mm2, -12.8% vs wave-3) UNDER the stricter D13 law** (non-passive >= 1.5mm, >= 9-pin >= 2.0mm, test points exempt — user decrees). Root cause of the 0.11mm: _pack_edges' _r5 half-mm block snap (a THIRD unrecorded quantization) stealing reserved pair gaps. _fanout_sep pair-exact (MAX not SUM, per-side inset credits, void-free), CW kernel unified, solver aprons exact — which REVEALED motor_sense had been silently on the legacy-fallback packer (the +_SEAT_SLIDE apron made the solver infeasible): restored, XT60 pair 1.0 -> 2.5, 12 contract drifts healed. Legalizer compaction pair-checked with fallback. Devkit: edge-vs-SoM fit + post-refit corridor eviction retire placement_mech (reds = {return_path} only); 105x105 (+floors cost real area on the dense little board — honest price). Naive control: 193x180 +10%, 6 starved, 18min. Exact: 183x164, 0 starved, 2:32 build. 6x byte-identical b291ce4f; 751 tests.
- WAVE-5 FIVE-THREAD BATCH (merged --no-ff x5: 737ed08 densify, d957176 seatperf, 72a524b testperf, 6d0788d hotpath, 06de926 sompose; renders 88ee124): board 190x170 -> **188x168 (31,584mm2, -8.2% vs wave-3's 34,410)**, pcb b1531332. DENSIFY: BL candidate packer (same _free legality; row/GRID/wrap slack killed), interior fill 49->54%, est -6% (board flipped budget-bound -> PACK-bound), zones emit at EXACT poses (snap-erosion class closed; 2 master-latent defects fixed: breathe stayer-need guard, edge-run END reach), _reorder_interchangeable swap-descent (debug_boot fan 2->0, board 421->266 crossings). SEAT-SOLVER ~5x (union lower bound + inlined DFS, node-count-identical, SCHGEN_SEAT_TRACE dual kernel). TESTS ~9x (shared session build + xdist loadgroup; 1569s -> ~150s; pytest-xdist new dev dep). HOT-PATHS: npp/mst computed once + threaded, silk _BoxIndex (21.6M -> 0.3M tests), bit-identical fold proof. POSE: (-10,+6) re-proven on the densified engine (-336mm2). Build ~206s + suite ~90s. NEXT CAMPAIGN (measured, blocked): 176x165 (-10.1%) behind ONE un-root-caused 0.11mm reach-composition shortfall (J17001/C32001, suspects recorded in densify_result.md); also flagged: board_parts() double compute, block_diagram.svg REPO_ROOT clobber, _pack_connector_zone THT-blocker frame mismatch (unexercised).
- WAVE-4 MULTI-SHAPE BLOCKS (merged --no-ff 27d0d88): board 186x185 -> **190x170 (-6.1% area, 34,410 -> 32,300 mm2)**. Every interior block now offers a small legal shape set (contracted: {as-built, turned} via the L1 arm; shelf-packed: real re-packs at aspects 2.2/1.0/0.45); _attempt_pack chooses greedily per block (fixed order, city-block fit, index tiebreak — deterministic, no search tree). 6/18 blocks chose non-default; bringup_rails stood UP beside the SoM (the exact wall the wave-3 exhaustive proof named). Chosen-shape plumbing end-to-end: apply_chosen_shapes rebinds emission views; LAW-5 estimator + P6 legalizer judge per-chosen-shape geometry; per-shape D13 reach; loud AssertionError on unregistered indices. Shape-stripped control reproduced master byte-identically (plumbing proven inert). Cross-machine identity: worker + orchestrator builds both converge to pcb d21be1a2. Airwire 16,932.5 -> 17,084.1 (+0.9%) inside the (now tighter) LAW-5 budget. Floorplan phase +9.1%. 746 tests + ruff green; devkit 110x84 -> 105x87 with the same 4 authoring reds. Goldens re-blessed (3d views only — zero schematic drift). Finding en route: refit-capable sheets (external.downstream, no seated connector) were never pose-predictable; emit_mobile_sheets now derives a "refit" reason from same-run contracts and the exactness suite treats them as position-dependent, like L4 movers. Product path proven untouched by byte-identical rebuild.
- NE-QUADRANT SPREAD: REFUTED by measurement (2 variants). usb_jtag near-connector pull = exact no-op (byte-identical board). Vertical family chain (board_aux NE + bringup_en_modules N-bias) = 197x185 (+5.9% area), airwire +0.4%, and the clique resisted (affinity outweighs weak side hints; en_modules went SW). Combined with wave-3's three-way control on bringup_rails (flat strip = the packable shape), vertical family spread is refuted at both the data and shape level. The NE vacuum is rectangular-outline slack bounded by the EDGE RUNS, not interior waste — filling it costs area and buys nothing measured. Remaining unexplored lever if congestion ever matters: a density-relaxation term with strict area/airwire guards.
- SIZING LEVERS (2026-07-29, from the edge-search residuals): (b) est-vs-real bias LANDED as a STRUCTURAL estimator — the floorplan's LAW-5 sizing estimate is now the ratsnest gate's OWN kernel (ratsnest._mst_edges Manhattan-select Prim, Euclidean cross-edge sum) on the STEP-3 PAD population (grid-snapped zone origins + the shared packer's offsets, SoM receptacles + under-SoM decoupling grid, corner holes, LAW-6 edge seats), rebuilt per candidate from the same zone geometry the PCB places; the block-centre proxy and BOTH measured constants (PROXY_TO_REAL, L4_PULL_CREDIT) are DELETED (they were learned-from-prior-builds numbers). Divergence decomposed at the winner: block-centre coarsening +4.8%, Manhattan-vs-Euclid select +0.3%, post-passes (L4/breathe/refit) -2.2%; est 17,883 -> 17,607 vs real 17,192 (bias +4.02% -> +2.41%, residual = the placer's own later refinement, correctly left as gate-side headroom — no constant). Population equivalence proven: 452 circuit nets == merged netlist pin-for-pin, 0 phantom multi-sheet locals. Cost +0.9s on the 111s floorplan phase (0.8%, guard <10%): build 0.20s + 25 evals x 24ms. Board BYTE-IDENTICAL (13cadc65), 24 gates green, two-build deterministic, targeted tests 80 pass, ruff clean. (a) anchored-search widening REFUTED on this spec with a complete proof: est never binds — the anchor was 215x185 (window w 175-215, h 145-185, floor never touched), and an exhaustive 1mm-grid scan of EVERY landscape board below 34,615mm2 (w 170-300, h 110-200, 2,662 candidates incl. the unexplored w>215 wedge, 164s) packs ZERO, so 215x161 is the GLOBAL pack-minimum; the wall is interior shape (the 23.9x55.2 tall block, free area 16.3k >> 1.3k needed, pure fragmentation) + edge/corner geometry, not search reach. Widening/restarting the window buys 0mm2 on this spec and only adds wall — documentation, not code (eth-N variant claim not re-tested: that spec is not in the tree).
- BOARD-SHRINK WAVE (user-originated): ethernet moved to the W edge top-left slot -> 215x161 (34,615mm2, -3.1% area vs 214x167), cross-airwire 17,192 (-1.7%), ALL 24 gates green. Unblocked by a THERMAL-VIA fix: the via placer gave up when curated sites were blocked, leaving U22004 at 5/6 GND vias <=5.2mm and withholding pour credit from all three LM61460s; embed.py now walks an exhaustive nearest-first lattice (THERMAL_VIA_LATTICE_PITCH) inside the part's own pour — requirement/window untouched. Edge search (10 assignments) confirmed eth-W optimal; rj45 MUST be slot 0 of W (last slot = +25.8% area); pwm-S is hard-infeasible (motor_sense->motor_pwm <=20mm legalized per pack).
- SHELVED, NOT LANDED — edge-axis transposition: the finding is REAL (E/W edge zones pack wide-along-local-X = deep into the board; bringup_rails 68.5mm deep x 19.3 span, fmc 61.6x18.7) but the implemented fix measured NET NEGATIVE — alone: 201x179 (+0.7% area, airwire -2.5%); with eth-W: same 215x161 but airwire 17,232 vs 17,192 WITHOUT it. The deep offenders live on the CONTRACTED-sheet path (stage_templates) which the connector packer never touches — that is where a future attempt must aim. Diff preserved in scratchpad salvage_edgeaxis.diff.
- SECOND-TIER SCAN AUDIT (evening): zero sites qualified — silk 1.78s, ratsnest Prim 0.023s, fanout 0.02s, refdes 0.005s, all under the 2s bar, skipped with numbers (empty diff by finding; prototypes preserved in scratchpad). Real residual heat named: (a) ratsnest net_pad_positions x41 recompute ~7.6s/build — fix is a HOIST i.e. a cache, blocked on the no-memoization law, AWAITS USER AUTHORIZATION; (b) stage_templates seat-solver backtracking ~60s/124.8M overlaps — byte-pinned by search order, a campaign of its own.
- SPATIAL HASH LANDED (evening, redo-proven): the earlier "identity failure" root cause was NOT a code leak — carrier/escape_block.json is a build OUTPUT read back as INPUT, and the compared builds straddled sidecar generations (the torn pair is visible inside 70d4139 itself: FLOORPLAN.md 214x167 beside a 215x167 PCB). With sidecar states pinned: pristine==hash byte-for-byte on BOTH states, zero trace divergence, 2.11x pcb-phase / 1.86x full build (orchestrator-measured 328s), 84 worker tests + 7 landed tests green. Sidecar-loop explicitness chipped (task_813fb8b9). Supersedes the earlier "semantic leak suspected" note.
- RECORD CORRECTION (2026-07-28 evening): commit 70d4139's message overclaims — the spatial-hash kernel was NOT in that commit (a silently-failed apply, caught post-push). 70d4139 actually landed: edges-as-membership + edge_order pinning with MEASURED orders (S tx-first + W lcd-first, user-driven, cross-airwire 17494.7->17243.0), coverage lint (231/0/88 baseline), artifacts. The hash REDO is tracked separately: on this tree the worker's hunks produced a trace-divergence-free build whose BOARD still changed (md5 46b2eecf->65fc1702, 17243->17496.5) = a semantic leak outside the traced kernel (far_ceil/max_reach threading x legalizer-fix interaction suspected); identity gate failed -> not landed. Pipeline hardening adopted: every apply verified by signature-grep before anything downstream.
- VISUAL AUDIT CAMPAIGN (2026-07-28, user-driven): all 23 contract subsections inspected (3D crops + per-sheet ratsnest + MST distance tables). 3 rounds landed: 25+ new structures across 12 sheets (motor in-line chain, lcd boost train 37.5->5.2mm, usb_jtag crystal loop 14->3mm, pd_input analog straps, hdmi_tx flow-through, pmod banks, uart_bridge seat 125->43mm, camera/usbc/microsd/fmc/pmodx). Engine: structure-derived member side-override; floorplan overlap REJECTION (legalizer accepted overlapping rects — deep fix chipped); seam quantization guard; seat-kernel pad faces; snap-erosion allowance (>=5 tier; 3.0-tier variant measured catastrophic 239x239). Harness board-frame parity (worker-delivered, reviewed): conn_rot+outer_dir+facing threaded, 23/23 parity. usb_pd facing generalization (worker): NOT landed — kernel sim -1.0mm only, no present beneficiary; design + caveats recorded in scratchpad usb_pd_facing_result.md, future item. Rejections that mattered: usb_jtag seat-pull (385x318 explosion; audit-A defensible-trade verdict upheld). FINAL: 204x191, DRC 0, contract 148/0, fan-out 110/0, airwire 17319 (improved), all gates green, 48 targeted tests pass.
- FINAL GATE: full pytest suite 720 passed / 0 failed (311f120) — 7 pre-wire-world tests re-pinned to full-wire invariants (red-on-before -> holds-on-board; participants 23-wired/guarded-empty; bottom-convention worst re-measured).
- U3a FULL WIRE GREEN: 22 wired sheets, EXIT 0 (DRC 0, contract 0/106, fan-out 0/110 starved, silk 0, LAW-5/6 PASS) at 214x165. Convergence mechanized 6 engine defects, each measured on the emitted board, never softened a gate:
  1. zone-frame rotation: conn roots must be BUILT at their final LAW-6 rotation (connector_edge_rotation) or every solved adjacency shatters when placement rotates them (camera 3.7mm vs 8mm min_from).
  2. double-rotation: solver rot_out must EXCLUDE mating connectors (placement ADDS zone_extra_rot to conn_rot: 180+180=0 emitted, 8 connectors mouth-inward) — two emitter sites, both guarded.
  3. seat-line replication: the edge-seat overwrites the conn's perpendicular coordinate (pads at EDGE_PAD_CLEAR from the edge). The zone must put its outer boundary exactly there; the mouth overhangs off-zone as it overhangs off-board (DS1024 9mm mouth in-extents left ESD 13.5mm from pads; 11 violations, one mechanism).
  4. compose inboard clamp: clusters WITHOUT a connector must stay inboard of the aligned pad line (LDO cluster at min=0 rode the seat-line shift 1.0mm off Edge.Cuts).
  5. fan-out reach formula: max(0, margin-GRID) credit under-reserves whenever margin < GRID; straight need+GRID-margin (clamped at the end) — emitted blocks overlapped 0.18mm through the old credit.
  6. symmetric apron: a multi-pin member placed near a 2-pin NON-exempt crowder (shunt-anchored INA3221) must carry its OWN intelligent_need; only the reverse direction was enforced.
  Silk: declutter accepted exact tangency; gate re-read it as overlap at 1e-14. 0.02mm guard band in both placers (keep-site + relocation search).
- U3a: W1 (4 sheets) green after mechanizing the LAW-6 seat-sweep exclusion (DRC 28->0); coverage 19->14; FULL WIRE build launched (15 more sheets).
- U2a DONE: defect corpus 13/13. U4a implemented (board_verdicts.json).
- U1a-d DONE: project spec v1 (5 extractions), byte-identical e405097b.
- 2026-07-09: branch created off master 8b036b1; tracker initialized.
