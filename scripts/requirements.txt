# Dependencies for the scripts/carrier hierarchical schematic generator.
#
# Pinned to versions verified to work with the generator. Install into a
# venv at the repo root:
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r scripts/requirements.txt
#
# Then run the generator with:
#
#   .venv/bin/python -m scripts.carrier

kicad-sch-api==0.5.6
kicad-skip==0.2.5
pytest==9.0.3
