schgen design-rule completeness gate
================================================================

rules (model-only, pin FUNCTION inferred by NAME):
  DECAP every multi-pin IC supply pin has a cap to GND on its sheet
  I2C   every i2c-typed bus net has a pull-up to a rail (board-wide)
  RESET every reset net carries an RC (cap-to-GND + pull resistor)
  STRAP no config-input pin floats on a passive-only undriven net
  EP    every exposed/thermal pad is netted to a GROUND net (never nc/floating, never a non-GND net)

checked: 40 IC supply pins, 10 i2c nets, 3 reset nets, 0 config pins, 5 exposed pads

DECAP — missing decoupling (0):
  (none)

I2C — i2c bus with no pull-up (0):
  (none)

RESET — reset without a full RC (0):
  (none)

STRAP — floating control input (0):
  (none)

EP — exposed pad not on GND (0):
  (none)

waivers — author-declared, verbatim (7):
  DECAP board_services:U2.VBACKUP (V_RTC_BAT): VBACKUP is the RV-3028 coin-cell backup input (a rechargeable ML1220, not a switching rail); the RTC regulates internally and a cap on the cell net is optional — no bypass fitted by design
  DECAP board_services:U2.VDD (+3V3_AUX): VBACKUP is the RV-3028 coin-cell backup input (a rechargeable ML1220, not a switching rail); the RTC regulates internally and a cap on the cell net is optional — no bypass fitted by design
  I2C ZYNQ_HDMI_TX_SCL (typed on hdmi_tx): DDC pull-ups integrated in TPD12S016 (DS 7.3.9/7.3.15)
  I2C ZYNQ_HDMI_TX_SDA (typed on hdmi_tx): DDC pull-ups integrated in TPD12S016 (DS 7.3.9/7.3.15)
  RESET CP2102N_RST_N (on uart_bridge): open-drain RST: 1k pull-up only, internal POR; no RC cap
  RESET LCD_CTP_RST (on lcd): GPIO-driven reset, held by 100k pull-down until PL releases
  RESET STM32_NRST (on bringup_rails): internal-pull whitelist (part provides its own pull; external RC optional)

DESIGN RULES: PASS (0 findings, 7 waived)
