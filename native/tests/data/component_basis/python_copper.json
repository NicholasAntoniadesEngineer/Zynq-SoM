[
  {
    "name": "unmeasured",
    "result": {
      "entries": [
        {
          "eid": "CD-01",
          "title": "LM61460 pour-aware effective RthJA (58.7 -> 35 C/W)",
          "assumes": "full-board In1.Cu GND plane + per-buck thermal-via field (>=6 x 0.6/0.3) at PGND1/PGND2 + local F.Cu/B.Cu GND pours (TI SNVSBD5D 7.3 bare 58.7 C/W vs 25 C/W on a 4-layer board; 11.1.1 layout)",
          "where": [
            "schgen/verify/thermal.py:66",
            "carrier/subsystems/power.py:13"
          ],
          "emits": "UNMEASURED — no emitted board scanned",
          "status": "UNMEASURED",
          "risk": "without this copper Tj(power:U1, 2.42 W) backs out to ~192 C at the bare 58.7 C/W against the 140 C guard — a board-dead thermal PASS-on-fiction (the GAP1 CRITICAL). The thermal gate now HARD-verifies this copper per build."
        },
        {
          "eid": "CD-02",
          "title": "TLV75725 DYD EP thermal-pad RthJA (92.5 C/W)",
          "assumes": "DYD thermal pad soldered to GND copper + JESD51-5 pad-adjacent thermal vias into the buried plane (the DS DYD RthJA ~92.5 C/W is DEFINED on that stackup; without it the gate falls back to the DBV bare 231 C/W)",
          "where": [
            "schgen/verify/thermal.py:99"
          ],
          "emits": "UNMEASURED — no emitted board scanned",
          "status": "UNMEASURED",
          "risk": "at the no-copper fallback 231 C/W the VADJ LDO (fmc:U1, 320 mW) lands Tj ~124 C over its 115 C guard — the PWR-3 package swap's entire benefit rides on this copper"
        },
        {
          "eid": "CD-03",
          "title": "TPS26631 PWP 2s2p RthJA (33.6 C/W)",
          "assumes": "HTSSOP-20 PowerPAD EP soldered to copper with thermal vias into a buried plane (TI SLVSE94 RthJA ~33.6 C/W is the JEDEC 2s2p figure — it presumes internal planes)",
          "where": [
            "schgen/verify/thermal.py:92"
          ],
          "emits": "UNMEASURED — no emitted board scanned",
          "status": "UNMEASURED",
          "risk": "low today (Pd ~54 mW -> huge margin even at several times the 2s2p figure), but the basis string was still plane-predicated while no plane existed"
        },
        {
          "eid": "CD-04",
          "title": "CHASSIS_GND island + single-point GND bond",
          "assumes": "a CHASSIS_GND copper island (connector shells + mounting ring) joined to signal GND at EXACTLY ONE point (bonding pad / 0R / via stitch near power entry)",
          "where": [
            "carrier/subsystems/mechanical/mechanical.py:9"
          ],
          "emits": "UNMEASURED — no emitted board scanned",
          "status": "UNMEASURED",
          "risk": "fab'd as-is the shields/mounting ring have NO DC reference (ESD return path open) — ERC/DRC cannot see it because the single-point bond is deliberately not netlisted"
        },
        {
          "eid": "CD-05",
          "title": "Bob-Smith trunk copper (BS_COMMON island)",
          "assumes": "a Bob-Smith common trunk (4 x 75R || 1n/2kV into BS_COMMON) laid in copper on the chassis-side island, spaced for the 2kV surge rating (IEEE 802.3 40.7.1)",
          "where": [
            "subsystems/ethernet/ethernet.py:69"
          ],
          "emits": "UNMEASURED — no emitted board scanned",
          "status": "UNMEASURED",
          "risk": "without trunk copper + spacing the 2kV HF termination exists only as parts; surge creepage and return-path behaviour are unproven until the routing wave draws the island"
        },
        {
          "eid": "CD-06",
          "title": "Ethernet isolation moat (In1 plane voids)",
          "assumes": "NO GND plane under the ethernet magnetics line side / RJ45 media pins (Pulse HX5008 layout guidance; the 2kV isolation moat) — the full-board In1 plane must be VOIDED there",
          "where": [
            "subsystems/ethernet/ethernet.py:12",
            "schgen/generate/pcb/constants.py:179"
          ],
          "emits": "UNMEASURED — no emitted board scanned",
          "status": "UNMEASURED",
          "risk": "a continuous plane under the magnetics bridges the isolation barrier capacitively and violates the 2kV creepage intent; the RJ45<->magnetics media CORRIDOR void remains routing-wave debt"
        },
        {
          "eid": "CD-07",
          "title": "DP90/DP100 L2 reference plane",
          "assumes": "the DP90_USB / DP100_TMDS trace geometry (widths/gaps in the .kicad_dru + net classes) is an OUTER-layer microstrip referenced to a CONTINUOUS L2 GND plane through one 7628 prepreg sheet",
          "where": [
            "schgen/generate/constraints.py:22"
          ],
          "emits": "UNMEASURED — no emitted board scanned",
          "status": "UNMEASURED",
          "risk": "without the L2 plane every impedance number in the dru is unmoored (return path undefined, impedance wrong by design); with the plane emitted the residual risk is fill VOIDS/splits under a future route path (checked at the routing wave)"
        },
        {
          "eid": "CD-08",
          "title": "Under-SoM decoupling fanout vias",
          "assumes": "the under-SoM bottom-side rail-entry decoupling (som_decoupling) reaches the rails/planes through fanout vias directly beneath the DF40 mezzanine",
          "where": [
            "schgen/generate/pcb/embed.py:510"
          ],
          "emits": "UNMEASURED — no emitted board scanned",
          "status": "UNMEASURED",
          "risk": "until those vias land the 18 bottom caps decouple nothing (no path from B.Cu pads to the rails) — a routing-wave item, but the prose reads as if the path exists"
        }
      ],
      "inventory": "NO BOARD SCANNED (statuses UNMEASURED)"
    },
    "report": "schgen copper-debt ledger — copper-predicated claims vs the EMITTED board\n==============================================================================\n\nEvery gate basis string / design-prose note that PRESUMES board copper,\nmeasured against what the emitter actually wrote into the .kicad_pcb this\nbuild. REPORT-ONLY (not a hard gate) — but the thermal gate HARD-verifies\nits pour credits against the same scan, so CD-01/CD-02 cannot pass on\nfiction regardless of this file. 'where' lines are anchor-resolved at report\ntime against STRUCTURE (code / a registered basis entry name); a vanished or\nprose-resolving anchor FAILS the build, never a stale line and never a marker.\n\nemitted copper inventory: NO BOARD SCANNED (statuses UNMEASURED)\n\nCD-01  LM61460 pour-aware effective RthJA (58.7 -> 35 C/W)   [UNMEASURED]\n  assumes: full-board In1.Cu GND plane + per-buck thermal-via field (>=6 x 0.6/0.3) at PGND1/PGND2 + local F.Cu/B.Cu GND pours (TI SNVSBD5D 7.3 bare 58.7 C/W vs 25 C/W on a 4-layer board; 11.1.1 layout)\n  where:   schgen/verify/thermal.py:66\n           carrier/subsystems/power.py:13\n  emits:   UNMEASURED — no emitted board scanned\n  risk:    without this copper Tj(power:U1, 2.42 W) backs out to ~192 C at the bare 58.7 C/W against the 140 C guard — a board-dead thermal PASS-on-fiction (the GAP1 CRITICAL). The thermal gate now HARD-verifies this copper per build.\n\nCD-02  TLV75725 DYD EP thermal-pad RthJA (92.5 C/W)   [UNMEASURED]\n  assumes: DYD thermal pad soldered to GND copper + JESD51-5 pad-adjacent thermal vias into the buried plane (the DS DYD RthJA ~92.5 C/W is DEFINED on that stackup; without it the gate falls back to the DBV bare 231 C/W)\n  where:   schgen/verify/thermal.py:99\n  emits:   UNMEASURED — no emitted board scanned\n  risk:    at the no-copper fallback 231 C/W the VADJ LDO (fmc:U1, 320 mW) lands Tj ~124 C over its 115 C guard — the PWR-3 package swap's entire benefit rides on this copper\n\nCD-03  TPS26631 PWP 2s2p RthJA (33.6 C/W)   [UNMEASURED]\n  assumes: HTSSOP-20 PowerPAD EP soldered to copper with thermal vias into a buried plane (TI SLVSE94 RthJA ~33.6 C/W is the JEDEC 2s2p figure — it presumes internal planes)\n  where:   schgen/verify/thermal.py:92\n  emits:   UNMEASURED — no emitted board scanned\n  risk:    low today (Pd ~54 mW -> huge margin even at several times the 2s2p figure), but the basis string was still plane-predicated while no plane existed\n\nCD-04  CHASSIS_GND island + single-point GND bond   [UNMEASURED]\n  assumes: a CHASSIS_GND copper island (connector shells + mounting ring) joined to signal GND at EXACTLY ONE point (bonding pad / 0R / via stitch near power entry)\n  where:   carrier/subsystems/mechanical/mechanical.py:9\n  emits:   UNMEASURED — no emitted board scanned\n  risk:    fab'd as-is the shields/mounting ring have NO DC reference (ESD return path open) — ERC/DRC cannot see it because the single-point bond is deliberately not netlisted\n\nCD-05  Bob-Smith trunk copper (BS_COMMON island)   [UNMEASURED]\n  assumes: a Bob-Smith common trunk (4 x 75R || 1n/2kV into BS_COMMON) laid in copper on the chassis-side island, spaced for the 2kV surge rating (IEEE 802.3 40.7.1)\n  where:   subsystems/ethernet/ethernet.py:69\n  emits:   UNMEASURED — no emitted board scanned\n  risk:    without trunk copper + spacing the 2kV HF termination exists only as parts; surge creepage and return-path behaviour are unproven until the routing wave draws the island\n\nCD-06  Ethernet isolation moat (In1 plane voids)   [UNMEASURED]\n  assumes: NO GND plane under the ethernet magnetics line side / RJ45 media pins (Pulse HX5008 layout guidance; the 2kV isolation moat) — the full-board In1 plane must be VOIDED there\n  where:   subsystems/ethernet/ethernet.py:12\n           schgen/generate/pcb/constants.py:179\n  emits:   UNMEASURED — no emitted board scanned\n  risk:    a continuous plane under the magnetics bridges the isolation barrier capacitively and violates the 2kV creepage intent; the RJ45<->magnetics media CORRIDOR void remains routing-wave debt\n\nCD-07  DP90/DP100 L2 reference plane   [UNMEASURED]\n  assumes: the DP90_USB / DP100_TMDS trace geometry (widths/gaps in the .kicad_dru + net classes) is an OUTER-layer microstrip referenced to a CONTINUOUS L2 GND plane through one 7628 prepreg sheet\n  where:   schgen/generate/constraints.py:22\n  emits:   UNMEASURED — no emitted board scanned\n  risk:    without the L2 plane every impedance number in the dru is unmoored (return path undefined, impedance wrong by design); with the plane emitted the residual risk is fill VOIDS/splits under a future route path (checked at the routing wave)\n\nCD-08  Under-SoM decoupling fanout vias   [UNMEASURED]\n  assumes: the under-SoM bottom-side rail-entry decoupling (som_decoupling) reaches the rails/planes through fanout vias directly beneath the DF40 mezzanine\n  where:   schgen/generate/pcb/embed.py:510\n  emits:   UNMEASURED — no emitted board scanned\n  risk:    until those vias land the 18 bottom caps decouple nothing (no path from B.Cu pads to the rails) — a routing-wave item, but the prose reads as if the path exists\n\nCOPPER DEBT: 8 entries — 0 emitted, 0 partial, 0 unemitted, 8 unmeasured (report-only)"
  },
  {
    "name": "carrier",
    "result": {
      "entries": [
        {
          "eid": "CD-01",
          "title": "LM61460 pour-aware effective RthJA (58.7 -> 35 C/W)",
          "assumes": "full-board In1.Cu GND plane + per-buck thermal-via field (>=6 x 0.6/0.3) at PGND1/PGND2 + local F.Cu/B.Cu GND pours (TI SNVSBD5D 7.3 bare 58.7 C/W vs 25 C/W on a 4-layer board; 11.1.1 layout)",
          "where": [
            "schgen/verify/thermal.py:66",
            "carrier/subsystems/power.py:13"
          ],
          "emits": "In1.Cu GND plane: YES; U20001: 8 GND vias<=5.2mm, F.Cu pour YES, B.Cu pour YES; U20002: 8 GND vias<=5.2mm, F.Cu pour YES, B.Cu pour YES; U22004: 8 GND vias<=5.2mm, F.Cu pour YES, B.Cu pour YES",
          "status": "EMITTED",
          "risk": "without this copper Tj(power:U1, 2.42 W) backs out to ~192 C at the bare 58.7 C/W against the 140 C guard — a board-dead thermal PASS-on-fiction (the GAP1 CRITICAL). The thermal gate now HARD-verifies this copper per build."
        },
        {
          "eid": "CD-02",
          "title": "TLV75725 DYD EP thermal-pad RthJA (92.5 C/W)",
          "assumes": "DYD thermal pad soldered to GND copper + JESD51-5 pad-adjacent thermal vias into the buried plane (the DS DYD RthJA ~92.5 C/W is DEFINED on that stackup; without it the gate falls back to the DBV bare 231 C/W)",
          "where": [
            "schgen/verify/thermal.py:99"
          ],
          "emits": "In1.Cu GND plane: YES; U11001: 3 GND vias<=3.0mm, F.Cu pour YES",
          "status": "EMITTED",
          "risk": "at the no-copper fallback 231 C/W the VADJ LDO (fmc:U1, 320 mW) lands Tj ~124 C over its 115 C guard — the PWR-3 package swap's entire benefit rides on this copper"
        },
        {
          "eid": "CD-03",
          "title": "TPS26631 PWP 2s2p RthJA (33.6 C/W)",
          "assumes": "HTSSOP-20 PowerPAD EP soldered to copper with thermal vias into a buried plane (TI SLVSE94 RthJA ~33.6 C/W is the JEDEC 2s2p figure — it presumes internal planes)",
          "where": [
            "schgen/verify/thermal.py:92"
          ],
          "emits": "In1.Cu GND plane: YES; U17001: 4 in-footprint EP via-pads (GND, PTH)",
          "status": "EMITTED",
          "risk": "low today (Pd ~54 mW -> huge margin even at several times the 2s2p figure), but the basis string was still plane-predicated while no plane existed"
        },
        {
          "eid": "CD-04",
          "title": "CHASSIS_GND island + single-point GND bond",
          "assumes": "a CHASSIS_GND copper island (connector shells + mounting ring) joined to signal GND at EXACTLY ONE point (bonding pad / 0R / via stitch near power entry)",
          "where": [
            "carrier/subsystems/mechanical/mechanical.py:9"
          ],
          "emits": "CHASSIS_GND board-level copper: 0 zones, 0 vias (pads only otherwise); no bond stitch emitted",
          "status": "NOTHING",
          "risk": "fab'd as-is the shields/mounting ring have NO DC reference (ESD return path open) — ERC/DRC cannot see it because the single-point bond is deliberately not netlisted"
        },
        {
          "eid": "CD-05",
          "title": "Bob-Smith trunk copper (BS_COMMON island)",
          "assumes": "a Bob-Smith common trunk (4 x 75R || 1n/2kV into BS_COMMON) laid in copper on the chassis-side island, spaced for the 2kV surge rating (IEEE 802.3 40.7.1)",
          "where": [
            "subsystems/ethernet/ethernet.py:69"
          ],
          "emits": "BS_COMMON board-level copper: 0 zones, 0 vias (pads only)",
          "status": "NOTHING",
          "risk": "without trunk copper + spacing the 2kV HF termination exists only as parts; surge creepage and return-path behaviour are unproven until the routing wave draws the island"
        },
        {
          "eid": "CD-06",
          "title": "Ethernet isolation moat (In1 plane voids)",
          "assumes": "NO GND plane under the ethernet magnetics line side / RJ45 media pins (Pulse HX5008 layout guidance; the 2kV isolation moat) — the full-board In1 plane must be VOIDED there",
          "where": [
            "subsystems/ethernet/ethernet.py:12",
            "schgen/generate/pcb/constants.py:179"
          ],
          "emits": "In1 rule-area voids: ethernet_isolation_void_J23001, ethernet_isolation_void_T10001; corridor between them: NOT voided (debt)",
          "status": "PARTIAL",
          "risk": "a continuous plane under the magnetics bridges the isolation barrier capacitively and violates the 2kV creepage intent; the RJ45<->magnetics media CORRIDOR void remains routing-wave debt"
        },
        {
          "eid": "CD-07",
          "title": "DP90/DP100 L2 reference plane",
          "assumes": "the DP90_USB / DP100_TMDS trace geometry (widths/gaps in the .kicad_dru + net classes) is an OUTER-layer microstrip referenced to a CONTINUOUS L2 GND plane through one 7628 prepreg sheet",
          "where": [
            "schgen/generate/constraints.py:22"
          ],
          "emits": "In1.Cu GND plane zone 25.5,25.5 -> 192.5,187.5 mm (unfilled-on-disk, DRC-refilled); ethernet voids punch it locally (CD-06)",
          "status": "EMITTED",
          "risk": "without the L2 plane every impedance number in the dru is unmoored (return path undefined, impedance wrong by design); with the plane emitted the residual risk is fill VOIDS/splits under a future route path (checked at the routing wave)"
        },
        {
          "eid": "CD-08",
          "title": "Under-SoM decoupling fanout vias",
          "assumes": "the under-SoM bottom-side rail-entry decoupling (som_decoupling) reaches the rails/planes through fanout vias directly beneath the DF40 mezzanine",
          "where": [
            "schgen/generate/pcb/embed.py:510"
          ],
          "emits": "GND vias inside the SoM body zone: 7 (SoM zone found); rail fanout vias: none emitted",
          "status": "PARTIAL",
          "risk": "until those vias land the 18 bottom caps decouple nothing (no path from B.Cu pads to the rails) — a routing-wave item, but the prose reads as if the path exists"
        }
      ],
      "inventory": "8 fill zones (with In1.Cu GND plane), 3 rule areas, 34 vias (34 GND), 9 segments"
    },
    "report": "schgen copper-debt ledger — copper-predicated claims vs the EMITTED board\n==============================================================================\n\nEvery gate basis string / design-prose note that PRESUMES board copper,\nmeasured against what the emitter actually wrote into the .kicad_pcb this\nbuild. REPORT-ONLY (not a hard gate) — but the thermal gate HARD-verifies\nits pour credits against the same scan, so CD-01/CD-02 cannot pass on\nfiction regardless of this file. 'where' lines are anchor-resolved at report\ntime against STRUCTURE (code / a registered basis entry name); a vanished or\nprose-resolving anchor FAILS the build, never a stale line and never a marker.\n\nemitted copper inventory: 8 fill zones (with In1.Cu GND plane), 3 rule areas, 34 vias (34 GND), 9 segments\n\nCD-01  LM61460 pour-aware effective RthJA (58.7 -> 35 C/W)   [EMITTED]\n  assumes: full-board In1.Cu GND plane + per-buck thermal-via field (>=6 x 0.6/0.3) at PGND1/PGND2 + local F.Cu/B.Cu GND pours (TI SNVSBD5D 7.3 bare 58.7 C/W vs 25 C/W on a 4-layer board; 11.1.1 layout)\n  where:   schgen/verify/thermal.py:66\n           carrier/subsystems/power.py:13\n  emits:   In1.Cu GND plane: YES; U20001: 8 GND vias<=5.2mm, F.Cu pour YES, B.Cu pour YES; U20002: 8 GND vias<=5.2mm, F.Cu pour YES, B.Cu pour YES; U22004: 8 GND vias<=5.2mm, F.Cu pour YES, B.Cu pour YES\n  risk:    without this copper Tj(power:U1, 2.42 W) backs out to ~192 C at the bare 58.7 C/W against the 140 C guard — a board-dead thermal PASS-on-fiction (the GAP1 CRITICAL). The thermal gate now HARD-verifies this copper per build.\n\nCD-02  TLV75725 DYD EP thermal-pad RthJA (92.5 C/W)   [EMITTED]\n  assumes: DYD thermal pad soldered to GND copper + JESD51-5 pad-adjacent thermal vias into the buried plane (the DS DYD RthJA ~92.5 C/W is DEFINED on that stackup; without it the gate falls back to the DBV bare 231 C/W)\n  where:   schgen/verify/thermal.py:99\n  emits:   In1.Cu GND plane: YES; U11001: 3 GND vias<=3.0mm, F.Cu pour YES\n  risk:    at the no-copper fallback 231 C/W the VADJ LDO (fmc:U1, 320 mW) lands Tj ~124 C over its 115 C guard — the PWR-3 package swap's entire benefit rides on this copper\n\nCD-03  TPS26631 PWP 2s2p RthJA (33.6 C/W)   [EMITTED]\n  assumes: HTSSOP-20 PowerPAD EP soldered to copper with thermal vias into a buried plane (TI SLVSE94 RthJA ~33.6 C/W is the JEDEC 2s2p figure — it presumes internal planes)\n  where:   schgen/verify/thermal.py:92\n  emits:   In1.Cu GND plane: YES; U17001: 4 in-footprint EP via-pads (GND, PTH)\n  risk:    low today (Pd ~54 mW -> huge margin even at several times the 2s2p figure), but the basis string was still plane-predicated while no plane existed\n\nCD-04  CHASSIS_GND island + single-point GND bond   [NOTHING]\n  assumes: a CHASSIS_GND copper island (connector shells + mounting ring) joined to signal GND at EXACTLY ONE point (bonding pad / 0R / via stitch near power entry)\n  where:   carrier/subsystems/mechanical/mechanical.py:9\n  emits:   CHASSIS_GND board-level copper: 0 zones, 0 vias (pads only otherwise); no bond stitch emitted\n  risk:    fab'd as-is the shields/mounting ring have NO DC reference (ESD return path open) — ERC/DRC cannot see it because the single-point bond is deliberately not netlisted\n\nCD-05  Bob-Smith trunk copper (BS_COMMON island)   [NOTHING]\n  assumes: a Bob-Smith common trunk (4 x 75R || 1n/2kV into BS_COMMON) laid in copper on the chassis-side island, spaced for the 2kV surge rating (IEEE 802.3 40.7.1)\n  where:   subsystems/ethernet/ethernet.py:69\n  emits:   BS_COMMON board-level copper: 0 zones, 0 vias (pads only)\n  risk:    without trunk copper + spacing the 2kV HF termination exists only as parts; surge creepage and return-path behaviour are unproven until the routing wave draws the island\n\nCD-06  Ethernet isolation moat (In1 plane voids)   [PARTIAL]\n  assumes: NO GND plane under the ethernet magnetics line side / RJ45 media pins (Pulse HX5008 layout guidance; the 2kV isolation moat) — the full-board In1 plane must be VOIDED there\n  where:   subsystems/ethernet/ethernet.py:12\n           schgen/generate/pcb/constants.py:179\n  emits:   In1 rule-area voids: ethernet_isolation_void_J23001, ethernet_isolation_void_T10001; corridor between them: NOT voided (debt)\n  risk:    a continuous plane under the magnetics bridges the isolation barrier capacitively and violates the 2kV creepage intent; the RJ45<->magnetics media CORRIDOR void remains routing-wave debt\n\nCD-07  DP90/DP100 L2 reference plane   [EMITTED]\n  assumes: the DP90_USB / DP100_TMDS trace geometry (widths/gaps in the .kicad_dru + net classes) is an OUTER-layer microstrip referenced to a CONTINUOUS L2 GND plane through one 7628 prepreg sheet\n  where:   schgen/generate/constraints.py:22\n  emits:   In1.Cu GND plane zone 25.5,25.5 -> 192.5,187.5 mm (unfilled-on-disk, DRC-refilled); ethernet voids punch it locally (CD-06)\n  risk:    without the L2 plane every impedance number in the dru is unmoored (return path undefined, impedance wrong by design); with the plane emitted the residual risk is fill VOIDS/splits under a future route path (checked at the routing wave)\n\nCD-08  Under-SoM decoupling fanout vias   [PARTIAL]\n  assumes: the under-SoM bottom-side rail-entry decoupling (som_decoupling) reaches the rails/planes through fanout vias directly beneath the DF40 mezzanine\n  where:   schgen/generate/pcb/embed.py:510\n  emits:   GND vias inside the SoM body zone: 7 (SoM zone found); rail fanout vias: none emitted\n  risk:    until those vias land the 18 bottom caps decouple nothing (no path from B.Cu pads to the rails) — a routing-wave item, but the prose reads as if the path exists\n\nCOPPER DEBT: 8 entries — 4 emitted, 2 partial, 2 unemitted, 0 unmeasured (report-only)"
  },
  {
    "name": "devkit_mini",
    "result": {
      "entries": [
        {
          "eid": "CD-01",
          "title": "LM61460 pour-aware effective RthJA (58.7 -> 35 C/W)",
          "assumes": "full-board In1.Cu GND plane + per-buck thermal-via field (>=6 x 0.6/0.3) at PGND1/PGND2 + local F.Cu/B.Cu GND pours (TI SNVSBD5D 7.3 bare 58.7 C/W vs 25 C/W on a 4-layer board; 11.1.1 layout)",
          "where": [
            "schgen/verify/thermal.py:66",
            "carrier/subsystems/power.py:13"
          ],
          "emits": "In1.Cu GND plane: YES; U4001: 8 GND vias<=5.2mm, F.Cu pour YES, B.Cu pour YES; U4002: 8 GND vias<=5.2mm, F.Cu pour YES, B.Cu pour YES; U6004: 8 GND vias<=5.2mm, F.Cu pour YES, B.Cu pour YES",
          "status": "EMITTED",
          "risk": "without this copper Tj(power:U1, 2.42 W) backs out to ~192 C at the bare 58.7 C/W against the 140 C guard — a board-dead thermal PASS-on-fiction (the GAP1 CRITICAL). The thermal gate now HARD-verifies this copper per build."
        },
        {
          "eid": "CD-02",
          "title": "TLV75725 DYD EP thermal-pad RthJA (92.5 C/W)",
          "assumes": "DYD thermal pad soldered to GND copper + JESD51-5 pad-adjacent thermal vias into the buried plane (the DS DYD RthJA ~92.5 C/W is DEFINED on that stackup; without it the gate falls back to the DBV bare 231 C/W)",
          "where": [
            "schgen/verify/thermal.py:99"
          ],
          "emits": "In1.Cu GND plane: YES; ",
          "status": "PARTIAL",
          "risk": "at the no-copper fallback 231 C/W the VADJ LDO (fmc:U1, 320 mW) lands Tj ~124 C over its 115 C guard — the PWR-3 package swap's entire benefit rides on this copper"
        },
        {
          "eid": "CD-03",
          "title": "TPS26631 PWP 2s2p RthJA (33.6 C/W)",
          "assumes": "HTSSOP-20 PowerPAD EP soldered to copper with thermal vias into a buried plane (TI SLVSE94 RthJA ~33.6 C/W is the JEDEC 2s2p figure — it presumes internal planes)",
          "where": [
            "schgen/verify/thermal.py:92"
          ],
          "emits": "In1.Cu GND plane: YES; U3001: 4 in-footprint EP via-pads (GND, PTH)",
          "status": "EMITTED",
          "risk": "low today (Pd ~54 mW -> huge margin even at several times the 2s2p figure), but the basis string was still plane-predicated while no plane existed"
        },
        {
          "eid": "CD-04",
          "title": "CHASSIS_GND island + single-point GND bond",
          "assumes": "a CHASSIS_GND copper island (connector shells + mounting ring) joined to signal GND at EXACTLY ONE point (bonding pad / 0R / via stitch near power entry)",
          "where": [
            "carrier/subsystems/mechanical/mechanical.py:9"
          ],
          "emits": "CHASSIS_GND board-level copper: 0 zones, 0 vias (pads only otherwise); no bond stitch emitted",
          "status": "NOTHING",
          "risk": "fab'd as-is the shields/mounting ring have NO DC reference (ESD return path open) — ERC/DRC cannot see it because the single-point bond is deliberately not netlisted"
        },
        {
          "eid": "CD-05",
          "title": "Bob-Smith trunk copper (BS_COMMON island)",
          "assumes": "a Bob-Smith common trunk (4 x 75R || 1n/2kV into BS_COMMON) laid in copper on the chassis-side island, spaced for the 2kV surge rating (IEEE 802.3 40.7.1)",
          "where": [
            "subsystems/ethernet/ethernet.py:69"
          ],
          "emits": "BS_COMMON board-level copper: 0 zones, 0 vias (pads only)",
          "status": "NOTHING",
          "risk": "without trunk copper + spacing the 2kV HF termination exists only as parts; surge creepage and return-path behaviour are unproven until the routing wave draws the island"
        },
        {
          "eid": "CD-06",
          "title": "Ethernet isolation moat (In1 plane voids)",
          "assumes": "NO GND plane under the ethernet magnetics line side / RJ45 media pins (Pulse HX5008 layout guidance; the 2kV isolation moat) — the full-board In1 plane must be VOIDED there",
          "where": [
            "subsystems/ethernet/ethernet.py:12",
            "schgen/generate/pcb/constants.py:179"
          ],
          "emits": "In1 rule-area voids: none; corridor between them: NOT voided (debt)",
          "status": "NOTHING",
          "risk": "a continuous plane under the magnetics bridges the isolation barrier capacitively and violates the 2kV creepage intent; the RJ45<->magnetics media CORRIDOR void remains routing-wave debt"
        },
        {
          "eid": "CD-07",
          "title": "DP90/DP100 L2 reference plane",
          "assumes": "the DP90_USB / DP100_TMDS trace geometry (widths/gaps in the .kicad_dru + net classes) is an OUTER-layer microstrip referenced to a CONTINUOUS L2 GND plane through one 7628 prepreg sheet",
          "where": [
            "schgen/generate/constraints.py:22"
          ],
          "emits": "In1.Cu GND plane zone 25.5,25.5 -> 124.5,124.5 mm (unfilled-on-disk, DRC-refilled); ethernet voids punch it locally (CD-06)",
          "status": "EMITTED",
          "risk": "without the L2 plane every impedance number in the dru is unmoored (return path undefined, impedance wrong by design); with the plane emitted the residual risk is fill VOIDS/splits under a future route path (checked at the routing wave)"
        },
        {
          "eid": "CD-08",
          "title": "Under-SoM decoupling fanout vias",
          "assumes": "the under-SoM bottom-side rail-entry decoupling (som_decoupling) reaches the rails/planes through fanout vias directly beneath the DF40 mezzanine",
          "where": [
            "schgen/generate/pcb/embed.py:510"
          ],
          "emits": "GND vias inside the SoM body zone: 7 (SoM zone found); rail fanout vias: none emitted",
          "status": "PARTIAL",
          "risk": "until those vias land the 18 bottom caps decouple nothing (no path from B.Cu pads to the rails) — a routing-wave item, but the prose reads as if the path exists"
        }
      ],
      "inventory": "7 fill zones (with In1.Cu GND plane), 1 rule areas, 31 vias (31 GND), 9 segments"
    },
    "report": "schgen copper-debt ledger — copper-predicated claims vs the EMITTED board\n==============================================================================\n\nEvery gate basis string / design-prose note that PRESUMES board copper,\nmeasured against what the emitter actually wrote into the .kicad_pcb this\nbuild. REPORT-ONLY (not a hard gate) — but the thermal gate HARD-verifies\nits pour credits against the same scan, so CD-01/CD-02 cannot pass on\nfiction regardless of this file. 'where' lines are anchor-resolved at report\ntime against STRUCTURE (code / a registered basis entry name); a vanished or\nprose-resolving anchor FAILS the build, never a stale line and never a marker.\n\nemitted copper inventory: 7 fill zones (with In1.Cu GND plane), 1 rule areas, 31 vias (31 GND), 9 segments\n\nCD-01  LM61460 pour-aware effective RthJA (58.7 -> 35 C/W)   [EMITTED]\n  assumes: full-board In1.Cu GND plane + per-buck thermal-via field (>=6 x 0.6/0.3) at PGND1/PGND2 + local F.Cu/B.Cu GND pours (TI SNVSBD5D 7.3 bare 58.7 C/W vs 25 C/W on a 4-layer board; 11.1.1 layout)\n  where:   schgen/verify/thermal.py:66\n           carrier/subsystems/power.py:13\n  emits:   In1.Cu GND plane: YES; U4001: 8 GND vias<=5.2mm, F.Cu pour YES, B.Cu pour YES; U4002: 8 GND vias<=5.2mm, F.Cu pour YES, B.Cu pour YES; U6004: 8 GND vias<=5.2mm, F.Cu pour YES, B.Cu pour YES\n  risk:    without this copper Tj(power:U1, 2.42 W) backs out to ~192 C at the bare 58.7 C/W against the 140 C guard — a board-dead thermal PASS-on-fiction (the GAP1 CRITICAL). The thermal gate now HARD-verifies this copper per build.\n\nCD-02  TLV75725 DYD EP thermal-pad RthJA (92.5 C/W)   [PARTIAL]\n  assumes: DYD thermal pad soldered to GND copper + JESD51-5 pad-adjacent thermal vias into the buried plane (the DS DYD RthJA ~92.5 C/W is DEFINED on that stackup; without it the gate falls back to the DBV bare 231 C/W)\n  where:   schgen/verify/thermal.py:99\n  emits:   In1.Cu GND plane: YES; \n  risk:    at the no-copper fallback 231 C/W the VADJ LDO (fmc:U1, 320 mW) lands Tj ~124 C over its 115 C guard — the PWR-3 package swap's entire benefit rides on this copper\n\nCD-03  TPS26631 PWP 2s2p RthJA (33.6 C/W)   [EMITTED]\n  assumes: HTSSOP-20 PowerPAD EP soldered to copper with thermal vias into a buried plane (TI SLVSE94 RthJA ~33.6 C/W is the JEDEC 2s2p figure — it presumes internal planes)\n  where:   schgen/verify/thermal.py:92\n  emits:   In1.Cu GND plane: YES; U3001: 4 in-footprint EP via-pads (GND, PTH)\n  risk:    low today (Pd ~54 mW -> huge margin even at several times the 2s2p figure), but the basis string was still plane-predicated while no plane existed\n\nCD-04  CHASSIS_GND island + single-point GND bond   [NOTHING]\n  assumes: a CHASSIS_GND copper island (connector shells + mounting ring) joined to signal GND at EXACTLY ONE point (bonding pad / 0R / via stitch near power entry)\n  where:   carrier/subsystems/mechanical/mechanical.py:9\n  emits:   CHASSIS_GND board-level copper: 0 zones, 0 vias (pads only otherwise); no bond stitch emitted\n  risk:    fab'd as-is the shields/mounting ring have NO DC reference (ESD return path open) — ERC/DRC cannot see it because the single-point bond is deliberately not netlisted\n\nCD-05  Bob-Smith trunk copper (BS_COMMON island)   [NOTHING]\n  assumes: a Bob-Smith common trunk (4 x 75R || 1n/2kV into BS_COMMON) laid in copper on the chassis-side island, spaced for the 2kV surge rating (IEEE 802.3 40.7.1)\n  where:   subsystems/ethernet/ethernet.py:69\n  emits:   BS_COMMON board-level copper: 0 zones, 0 vias (pads only)\n  risk:    without trunk copper + spacing the 2kV HF termination exists only as parts; surge creepage and return-path behaviour are unproven until the routing wave draws the island\n\nCD-06  Ethernet isolation moat (In1 plane voids)   [NOTHING]\n  assumes: NO GND plane under the ethernet magnetics line side / RJ45 media pins (Pulse HX5008 layout guidance; the 2kV isolation moat) — the full-board In1 plane must be VOIDED there\n  where:   subsystems/ethernet/ethernet.py:12\n           schgen/generate/pcb/constants.py:179\n  emits:   In1 rule-area voids: none; corridor between them: NOT voided (debt)\n  risk:    a continuous plane under the magnetics bridges the isolation barrier capacitively and violates the 2kV creepage intent; the RJ45<->magnetics media CORRIDOR void remains routing-wave debt\n\nCD-07  DP90/DP100 L2 reference plane   [EMITTED]\n  assumes: the DP90_USB / DP100_TMDS trace geometry (widths/gaps in the .kicad_dru + net classes) is an OUTER-layer microstrip referenced to a CONTINUOUS L2 GND plane through one 7628 prepreg sheet\n  where:   schgen/generate/constraints.py:22\n  emits:   In1.Cu GND plane zone 25.5,25.5 -> 124.5,124.5 mm (unfilled-on-disk, DRC-refilled); ethernet voids punch it locally (CD-06)\n  risk:    without the L2 plane every impedance number in the dru is unmoored (return path undefined, impedance wrong by design); with the plane emitted the residual risk is fill VOIDS/splits under a future route path (checked at the routing wave)\n\nCD-08  Under-SoM decoupling fanout vias   [PARTIAL]\n  assumes: the under-SoM bottom-side rail-entry decoupling (som_decoupling) reaches the rails/planes through fanout vias directly beneath the DF40 mezzanine\n  where:   schgen/generate/pcb/embed.py:510\n  emits:   GND vias inside the SoM body zone: 7 (SoM zone found); rail fanout vias: none emitted\n  risk:    until those vias land the 18 bottom caps decouple nothing (no path from B.Cu pads to the rails) — a routing-wave item, but the prose reads as if the path exists\n\nCOPPER DEBT: 8 entries — 3 emitted, 2 partial, 3 unemitted, 0 unmeasured (report-only)"
  }
]
