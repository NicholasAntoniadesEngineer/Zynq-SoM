# Zynq carrier acceptance TEST PLAN

> GENERATED by `schgen testplan` (schgen/testplan.py) — DO NOT EDIT.
> Regenerate: `PYTHONPATH=. python -m schgen testplan`. Every expected
> value, min/max limit, net and probe pad below is JOINED from the
> analytic spice gate (`schgen/spice.py`), the test-point coverage gate
> (`schgen/testpoints.py`) and the staged bring-up sequence (`schgen/manual.py`,
> `schgen/bringup_facts.py`) — no value is hand-typed and none is re-derived
> here. Limits that FAIL the spice gate would also fail the board build.

Procedure: work the stages in order (close one bring-up DIP, verify, move
on — see `carrier/docs/BRINGUP.md`). For each step probe the listed pad,
record the measurement in the **measured** column, and tick **pass?** when
the reading sits inside `[min .. max]`. A blank (unprogrammed) system
controller is fine for every electrical step; the I2C scan and functional
checks below need SC firmware running.

Source spice gate: 11 checks, analytic (closed-form linear solutions).

## Electrical acceptance — by bring-up stage

### Stage 1 — first power: PD + always-on +3V3_SC domain

| step | net | probe pad | expected | min | max | measured | pass? |
|---|---|---|---|---|---|---|---|
| 1.1 divider CP2102N_VBUS_SNS [abs-max at 5.25 V VBUS] | `CP2102N_VBUS_SNS` | — | 3.583 V | — | 5.8 V | `______` | [ ] |
| 1.2 divider CP2102N_VBUS_SNS [detect at 4.75 V VBUS] | `CP2102N_VBUS_SNS` | — | 3.2417 V | 3 V | — | `______` | [ ] |
| 1.3 divider PD_OVP_SET [guaranteed cutoff below the TVS VBR min] | `PD_OVP_SET` | — | 1.2698 V | 1.224 V | — | `______` | [ ] |
| 1.4 divider PD_OVP_SET [no false trip at 21 V (contract max)] | `PD_OVP_SET` | — | 1.0929 V | — | 1.176 V | `______` | [ ] |

<details><summary>step rationale (from the spice gate detail)</summary>

- **1.1 divider CP2102N_VBUS_SNS [abs-max at 5.25 V VBUS]** (uart_bridge): USB_UART_VBUS -[R2=22100R]- CP2102N_VBUS_SNS -[R3=47500R]- GND @ 5.25 V: CP2102N self-powered VBUS sense (DS 22k1/47k5 reference): must read VBUS-present yet never exceed the pin abs-max 5.8 V
- **1.2 divider CP2102N_VBUS_SNS [detect at 4.75 V VBUS]** (uart_bridge): USB_UART_VBUS -[R2=22100R]- CP2102N_VBUS_SNS -[R3=47500R]- GND @ 4.75 V: CP2102N self-powered VBUS sense (DS 22k1/47k5 reference): must read VBUS-present yet never exceed the pin abs-max 5.8 V
- **1.3 divider PD_OVP_SET [guaranteed cutoff below the TVS VBR min]** (pd_input): +VBUS_IN -[R3=100000R]- PD_OVP_SET -[R4=5490R]- GND @ 24.4 V: TPS26631 OVP set divider (pd_input): must NOT trip inside the valid 20 V +5% contract window yet MUST cut off before the SMBJ22A starts clamping (V_OVPR 1.2 V +/-2%, SLVSE94G)
- **1.4 divider PD_OVP_SET [no false trip at 21 V (contract max)]** (pd_input): +VBUS_IN -[R3=100000R]- PD_OVP_SET -[R4=5490R]- GND @ 21 V: TPS26631 OVP set divider (pd_input): must NOT trip inside the valid 20 V +5% contract window yet MUST cut off before the SMBJ22A starts clamping (V_OVPR 1.2 V +/-2%, SLVSE94G)

</details>

### Stage 2 — rails, one DIP at a time

| step | net | probe pad | expected | min | max | measured | pass? |
|---|---|---|---|---|---|---|---|
| 2.1 LM61460AANRJRR FB (+3V3_REG) | `+3V3_REG` | — | 3.32 V | 3.201 V | 3.399 V | `______` | [ ] |
| 2.2 LM61460AANRJRR FB (+5V_REG) | `+5V_REG` | — | 5.02 V | 4.85 V | 5.15 V | `______` | [ ] |
| 2.3 LM61460AANRJRR FB (+5V_SOM) | `+5V_SOM` | power_som:TP1 | 4.6538 V | 4.5105 V | 4.7895 V | `______` | [ ] |
| 2.4 EN clamp ceiling (U4) | `+VIN_SYS` | — | 5.042 V | — | 5.5 V | `______` | [ ] |
| 2.5 EN clamp turn-on (U4) | `+VIN_SYS` | — | 4.505 V | 1.5 V | — | `______` | [ ] |
| 2.6 divider PG_1V8_G [FET on at nominal rail] | `PG_1V8_G` | — | 1.7822 V | 1.45 V | 1.8 V | `______` | [ ] |

<details><summary>step rationale (from the spice gate detail)</summary>

- **2.1 LM61460AANRJRR FB (+3V3_REG)** (power): Vout = 1.0 * (1 + R4/R5 = 23200/10000) vs nominal 3.3 V +/-3%
- **2.2 LM61460AANRJRR FB (+5V_REG)** (power): Vout = 1.0 * (1 + R1/R2 = 40200/10000) vs nominal 5 V +/-3%
- **2.3 LM61460AANRJRR FB (+5V_SOM)** (power_som): Vout = 1.0 * (1 + R14/R15 = 47500/13000) vs nominal 4.65 V +/-3%
- **2.4 EN clamp ceiling (U4)** (power_som): +VIN_SYS=20V -[R12=10000R]- EN, D5=MMSZ5231B zener->GND: EN at VIN=21.0V (20V+5%) worst-case (Vz 5.355V) must stay <= the EN recommended-max 5.5V (no internal clamp, I_hys 1.55uA only — SLVSDG6C)
- **2.5 EN clamp turn-on (U4)** (power_som): +VIN_SYS=20V -[R12=10000R]- EN, D5=MMSZ5231B zener->GND: EN at VIN=4.75V (5V contract low) must exceed enable+margin 1.5V (threshold 1.21V typ, SLVSDG6C) so the always-on buck is sure to start
- **2.6 divider PG_1V8_G [FET on at nominal rail]** (power): +1V8 -[R7=1000R]- PG_1V8_G -[R8=100000R]- GND @ 1.8 V: +1V8 PG sense: gate divider must exceed the AO3400A max Vgs(th)=1.45 V so the PG LED is guaranteed on

</details>

### Stage 5 — boot modes, JTAG, SWD

| step | net | probe pad | expected | min | max | measured | pass? |
|---|---|---|---|---|---|---|---|
| 5.1 BOOT0 strap VIH | `BOOT0_SET` | — | 3.094 V | 2.31 V | 3.3 V | `______` | [ ] |

<details><summary>step rationale (from the spice gate detail)</summary>

- **5.1 BOOT0 strap VIH** (debug_boot): DIP closed: 3.3 V through R3=100R vs the SoM 1k5 pull-down (contract) -> must exceed STM32 VIH = 0.7*VDD = 2.31 V

</details>

## I2C-scan expectation (SC firmware running)

With the always-on `+3V3_SC` domain up and SC firmware scanning the bring-up
buses, exactly these 7-bit addresses must ACK. The addresses are derived
from the netlist straps (`schgen/bringup_facts.py`), not hand-typed.

| address | device | ref | when | ACK? |
|---|---|---|---|---|
| `0x40` | INA3221 rail monitor | `U1` | always-on | [ ] |
| `0x41` | INA3221 rail monitor | `U2` | always-on | [ ] |

Always-on set (with `+3V3_SC`): 0x40/0x41. Any EXTRA address, or any of these missing, means a strap or bus fault.

## Per-module functional checklist

Each module rail comes up behind its own current-limited load switch (Stage 4).
After enabling a module, confirm its status LED and the headline function.

| sheet | gated rail | status LED | functional check |
|---|---|---|---|

