{
  "source": "schgen/generate/subsystem_scaffold.py",
  "widget_cir": "* widget.cir — SPICE subckt for the widget subsystem.\n*\n* PROJECT-AGNOSTIC: the subckt pins are the subsystem's ABSTRACT interface, NOT\n* any board net. Values mirror widget.py one-for-one (parse_si compatible).\n*\n* Port order: VDD GND   (GND last by convention; add the rest)\n\n.subckt widget VDD GND\n* <passives mirroring the netlist, e.g.:>\n* C1 VDD GND 100n\n.ends widget\n\n.end\n",
  "writers": [
    "{name}.py",
    "__init__.py",
    "README.md",
    "{name}.cir",
    "test_{name}.py"
  ],
  "default_rails": [
    "+VDD",
    "GND"
  ],
  "collision_error": "already exists"
}
